{
    "buildCommand": "npm ci && npm run build",
    "installCommand": "npm ci",
    "framework": null,
    "routes": [
        {
            "src": "/[^.]+",
            "dest": "/",
            "status": 200
        }
    ]
}